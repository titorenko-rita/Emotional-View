
export interface PostRPII {
    id_group: number,
    mac: string,
    is_active: boolean
}

export interface PatchRPII {
    id: number,
    id_group: number,
    mac: string,
    is_active: boolean
}